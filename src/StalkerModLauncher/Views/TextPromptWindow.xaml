<Window x:Class="StalkerModLauncher.Views.TextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:StalkerModLauncher.Resources"
        Width="430"
        Height="220"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource WindowBackgroundBrush}"
        Foreground="{StaticResource TextBrush}"
        SourceInitialized="Window_OnSourceInitialized">
    <Border Margin="14"
            Padding="16"
            Background="{StaticResource PanelBrush}"
            BorderBrush="{StaticResource StrokeBrush}"
            BorderThickness="1"
            CornerRadius="6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="PromptText"
                       Foreground="{StaticResource MutedTextBrush}"/>
            <TextBox x:Name="ValueTextBox"
                     Grid.Row="1"
                     Margin="0,6,0,0"
                     KeyDown="ValueTextBox_OnKeyDown"/>
            <StackPanel Grid.Row="3"
                        Margin="0,14,0,0"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="{x:Static res:Strings.Common_Cancel}"
                        MinWidth="92"
                        Margin="0,0,8,0"
                        IsCancel="True"/>
                <Button Content="{x:Static res:Strings.Common_Done}"
                        MinWidth="92"
                        Style="{StaticResource PrimaryButtonStyle}"
                        IsDefault="True"
                        Click="DoneButton_OnClick"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
