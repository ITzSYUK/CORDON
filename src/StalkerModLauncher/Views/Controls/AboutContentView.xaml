<UserControl x:Class="StalkerModLauncher.Views.Controls.AboutContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:res="clr-namespace:StalkerModLauncher.Resources"
             Foreground="{DynamicResource TextBrush}">
    <UserControl.Resources>
        <Style x:Key="AboutSectionHeadingStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>
        <Style x:Key="AboutParagraphStyle" TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="LineHeight" Value="21"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>
        <Style x:Key="AboutQuestionHeadingStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,3"/>
        </Style>
        <Style x:Key="AboutAnswerStyle" TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="LineHeight" Value="21"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
        <Style x:Key="AboutFinalAnswerStyle"
               TargetType="TextBlock"
               BasedOn="{StaticResource AboutAnswerStyle}">
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
    </UserControl.Resources>
    <StackPanel>
        <TextBlock Text="{x:Static res:Strings.About_ProfileHeading}" Style="{DynamicResource AboutSectionHeadingStyle}"/>
        <TextBlock Text="{x:Static res:Strings.About_ProfileBody}" Style="{DynamicResource AboutParagraphStyle}"/>

        <TextBlock Text="{x:Static res:Strings.About_CreateHeading}" Style="{DynamicResource AboutSectionHeadingStyle}"/>
        <TextBlock Text="{x:Static res:Strings.About_CreateBody}" Style="{DynamicResource AboutParagraphStyle}"/>

        <TextBlock Text="{x:Static res:Strings.About_RegularStandaloneHeading}" Style="{DynamicResource AboutSectionHeadingStyle}"/>
        <TextBlock Text="{x:Static res:Strings.About_RegularStandaloneBody}" Style="{DynamicResource AboutParagraphStyle}"/>

        <TextBlock Text="{x:Static res:Strings.About_WorkspaceHeading}" Style="{DynamicResource AboutSectionHeadingStyle}"/>
        <TextBlock Text="{x:Static res:Strings.About_WorkspaceBody}" Style="{DynamicResource AboutParagraphStyle}"/>

        <TextBlock Text="{x:Static res:Strings.About_LaunchModesHeading}" Style="{DynamicResource AboutSectionHeadingStyle}"/>
        <TextBlock Text="{x:Static res:Strings.About_LaunchModesBody}" Style="{DynamicResource AboutParagraphStyle}"/>

        <TextBlock Text="{x:Static res:Strings.About_Mo2Heading}" Style="{DynamicResource AboutSectionHeadingStyle}"/>
        <TextBlock Text="{x:Static res:Strings.About_Mo2Body}" Style="{DynamicResource AboutParagraphStyle}"/>

        <TextBlock Text="{x:Static res:Strings.About_BrowserHeading}" Style="{DynamicResource AboutSectionHeadingStyle}"/>
        <TextBlock Text="{x:Static res:Strings.About_BrowserBody}" Style="{DynamicResource AboutParagraphStyle}"/>

        <TextBlock Text="{x:Static res:Strings.About_FaqHeading}" Style="{DynamicResource AboutSectionHeadingStyle}"/>
        <TextBlock Text="{x:Static res:Strings.About_QuestionModOrder}" Style="{DynamicResource AboutQuestionHeadingStyle}"/>
        <TextBlock Text="{x:Static res:Strings.About_AnswerModOrder}" Style="{DynamicResource AboutAnswerStyle}"/>

        <TextBlock Text="{x:Static res:Strings.About_QuestionWorkspaceSize}" Style="{DynamicResource AboutQuestionHeadingStyle}"/>
        <TextBlock Text="{x:Static res:Strings.About_AnswerWorkspaceSize}" Style="{DynamicResource AboutAnswerStyle}"/>

        <TextBlock Text="{x:Static res:Strings.About_QuestionSavesAndLogs}" Style="{DynamicResource AboutQuestionHeadingStyle}"/>
        <TextBlock Text="{x:Static res:Strings.About_AnswerSavesAndLogs}" Style="{DynamicResource AboutAnswerStyle}"/>

        <TextBlock Text="{x:Static res:Strings.About_QuestionDeleteProfile}" Style="{DynamicResource AboutQuestionHeadingStyle}"/>
        <TextBlock Text="{x:Static res:Strings.About_AnswerDeleteProfile}" Style="{DynamicResource AboutAnswerStyle}"/>

        <TextBlock Text="{x:Static res:Strings.About_QuestionStartFailure}" Style="{DynamicResource AboutQuestionHeadingStyle}"/>
        <TextBlock Text="{x:Static res:Strings.About_AnswerStartFailure}" Style="{DynamicResource AboutAnswerStyle}"/>

        <TextBlock Text="{x:Static res:Strings.About_QuestionRemoveMod}" Style="{DynamicResource AboutQuestionHeadingStyle}"/>
        <TextBlock Text="{x:Static res:Strings.About_AnswerRemoveMod}" Style="{DynamicResource AboutFinalAnswerStyle}"/>

        <TextBlock Text="{x:Static res:Strings.About_DisclaimerHeading}" Style="{DynamicResource AboutSectionHeadingStyle}" Margin="0,12,0,6"/>
        <TextBlock Text="{x:Static res:Strings.About_DisclaimerBody}" Style="{DynamicResource AboutFinalAnswerStyle}"/>
    </StackPanel>
</UserControl>
